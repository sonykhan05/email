RewriteEngine On
RewriteBase /

# If the request is not for an existing file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite to index.html to handle shortened URLs
RewriteRule ^([a-zA-Z0-9-]+)$ index.html [L] 